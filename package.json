{
  "name": "dnd-sessions-handler",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Small D&D tools for the group (Hebrew). First tool: Session Zero.",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.128.0",
    "@google/genai": "^2.24.0"
  }
}
